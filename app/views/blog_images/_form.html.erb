<%= form_with(model: blog_image, local: true) do |form| %>
  <% if blog_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog_image.errors.count, "error") %> prohibited this blog_image from being saved:</h2>

      <ul>
      <% blog_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
