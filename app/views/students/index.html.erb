<% provide(:title, "Students") %>
<% if @students.none? %>
	<%= link_to("You haven't got students yet! Add your first student here", new_student_path) %><br>
<% end %>
<% @students.each do |s| %>
	<ul>
		<li>
			<%= link_to s.last_name + ", " + s.first_name, student_path(s) %><br>
			<%= image_tag s.picture.url if s.picture? %>
		</li>
	</ul>
<% end %>
Average length of membership: <%= @average_membership_length %> days<br>
<%= link_to "(show deactivated students)", deactivated_students_path(@students) %><br>