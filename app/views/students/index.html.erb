<% @students.each do |s| %>
	<ul>
		<li>
			<%= link_to s.first_name + " " + s.last_name, student_path(s) %><br>
			<%= image_tag s.picture.url if s.picture? %>
			<%= s.club.name %>
		</li>
	</ul>
<% end %>