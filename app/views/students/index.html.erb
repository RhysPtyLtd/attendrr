<section class="studentList">
	<% provide(:title, "Students") %>
	<div class="container mt-5">
		<div class="row">
			<div class="col-12 d-flex justify-content-start  mt-2 mb-2">
						<h1 style="margin-bottom:0px;">Students List</h1>
			</div>
		</div>
		<% @students.each do |s| %>
		<div class="row">
			<div class="col-md-6 col-12 ">
				<%= link_to( student_path(s ),:class=>"student_div") do %>
				<div class="student d-flex justify-content-start align-content-center mt-2 mb-2">
					<div class="align-self-center m-2">
						<% if s.picture? %><img class="student_img align-self-center" <%= image_tag s.picture.url  %><% end %>
					</div>
					<div class="align-self-center pl-2">
						<span class="student_name"> <%= s.last_name + ", " + s.first_name %></span>
					</div>
				</div>
				<% end %>
			</div>
		</div>
		<% end %>

		<div class="row">
			<div class="col-12 col-md-6 mt-3 mb-3">
				Average length of membership: <%= @average_membership_length %> days<br>
			</div>
		</div>
		<div class="row">
			<div class="col-12 col-md-6 mt-3 mb-3">
				<%= link_to "(show deactivated students)", deactivated_students_path(@students) %><br>
			</div>
		</div>
	</div>

</section>
