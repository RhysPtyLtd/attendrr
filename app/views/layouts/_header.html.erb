

  <section>

    <nav class="navbar fixed-top navbar-expand-lg  bottom-border">

        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed " data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to image_tag("attendrr-master.png", :alt => 'attendrr', :class => 'logo'), root_path, :class => 'navbar-brand ' %>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse navbar-white d-md-flex justify-content-end" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav">


          </ul>
          <ul class="nav navbar-nav  navbar-right">
            <li class="nav-item active mr-2 ">
              <%= link_to "Home", root_path, class: "signupbox  " %>
            </li>
            <li class="nav-item  mr-2">
              <%= link_to "Help",   help_path , class: "signupbox  " %>
            </li>
            <% if logged_in? %>
              <% if current_club && current_club.admin? %>
              <li><%= link_to "Clubs", clubs_path, class: "signupbox " %></li>
              <% end %>
              <div style="display:inline" class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                  Account
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to "Profile", current_club %></li>
                  <li><%= link_to "Settings", edit_club_path(current_club) %></li>
                  <li class="divider"></li>
                  <li>
                    <%= link_to "Log out", logout_path, method: "delete" %>
                  </li>
                </ul>
              </div>
            <% else %>
            <li>
              <%= link_to "Sign up ", signup_path, class: "signupbox mr-md-2 " %>
            </li>
            <li>
              <%= link_to "Login", login_path, class: 'loginbox mx-2 ' %>
            </li>
              <% end %>

          </ul>

        </div><!-- /.navbar-collapse -->
    </nav>
  </section>
